import AccionBase from "../accionBase.js";
import Avion from "./avion.js";
/*
export default class teclasArribaAbajo extends AccionBase
{
    constructor(scene, x, y, avion){
        super(scene, x, y);
        console.log("TeclasArribaAbajo ha sido creado.");
        cursors = this.input.keyboard.createCursorKeys();


    };
preUpdate(t,dt)
{
    if (cursors.left.isDown)
{
    avion.setVelocityX(-160);
}
else if (cursors.right.isDown)
{
    avion.setVelocityX(160);
}
    
    
    
    /*this.input.keyboard.on('keydown-W', event =>
    {
        avion.movimientoArriba();
        console.log("Ariba.");

    });
    this.input.keyboard.on('keydown-S', event =>
    {
        console.log("Abajo.");
    });
}
}
*/